# Config
python-dotenv>=1.0.0

# Web search (Tavily)
tavily-python>=0.5.0

# LLM and agents (Planner, Wiki agent)
langgraph>=0.2.0
langchain-openai>=0.2.0
langchain-core>=0.3.0
langchain-mcp-adapters>=0.1.0

# MCP server (wiki)
fastmcp>=2.0.0
httpx>=0.27.0
beautifulsoup4>=4.12.0

# Wiki RAG: vector store and embeddings
chromadb>=0.4.0
langchain-chroma>=1.0.0
langchain-community>=0.3.0

# Bus schedule PDF extraction
pdfplumber>=0.11.0

# API
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
PyJWT>=2.8.0

# LangGraph persistent checkpoints (not bundled in langgraph core)
langgraph-checkpoint-sqlite>=2.0.0
aiosqlite>=0.20.0

# Tests
pytest>=8.0.0
